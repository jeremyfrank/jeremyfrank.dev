---
type Article = {
  title: string
  url: string
}

type Props = {
  articles: Article[]
}

const { articles } = Astro.props
---

<ul>
  {
    articles.map((article) => (
      <li>
        <a href={article.url}>{article.title}</a>
      </li>
    ))
  }
</ul>
