---
const { url, classes, width = 800, height = 400 } = Astro.props
---

<iframe
  src={url}
  width={width}
  height={height}
  frameborder="0"
  class:list={['rounded-md w-full', classes]}
  loading="lazy"></iframe>
