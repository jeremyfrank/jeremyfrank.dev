---
const url = new URL(Astro.request.url)
const isHome = url.pathname === '/' || url.pathname === '/index.html'
const LogomarkTagName = isHome ? 'div' : 'a'
const logomarkHref = !isHome ? '/' : null
const logomarkLabel = !isHome ? 'Home' : null
---

<header class="layout">
  <LogomarkTagName
    href={logomarkHref}
    class="logo-wrapper"
    aria-label={logomarkLabel}
  >
    <img src="/images/jf-logomark.svg" width="64" height="64" alt="" />
  </LogomarkTagName>
</header>

<style>
  header {
    padding-top: clamp(60px, 12dvh, 100px);
    padding-bottom: clamp(30px, 6dvh, 50px);

    @media (orientation: portrait) and (max-width: 600px) {
      padding-top: clamp(50px, 8dvh, 70px);
      padding-bottom: clamp(25px, 4dvh, 35px);
    }
  }

  .logo-wrapper {
    border-radius: 999px;
    box-shadow: 0 0 0 3px white;
    margin-right: auto;
    pointer-events: auto;

    @media (prefers-color-scheme: dark) {
      box-shadow: 0 0 0 3px rgb(24 24 27);
    }

    &:focus-visible {
      outline: none;
      box-shadow:
        0 0 0 2px white,
        0 0 0 5px rgb(59 130 246);

      @media (prefers-color-scheme: dark) {
        box-shadow:
          0 0 0 2px rgb(24 24 27),
          0 0 0 5px rgb(59 130 246);
      }
    }

    img {
      height: 3.5rem;
      width: 3.5rem;

      @media (prefers-color-scheme: dark) {
        filter: invert(1);
      }

      @media (min-width: 48em) {
        height: 4rem;
        width: 4rem;
      }
    }
  }
</style>
